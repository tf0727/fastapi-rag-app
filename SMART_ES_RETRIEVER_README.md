# 智能 Elasticsearch 检索工具说明

## 概述

`elasticsearch_retriever_tool` 已经升级为一个智能检索工具，它使用大语言模型（LLM）来分析用户查询意图，并自动选择最优的搜索策略和参数配置。

## 核心功能

### 1. 智能查询分析

工具会使用 LLM 分析用户的查询，并生成结构化的搜索参数，包括：

- **搜索类型选择**：自动判断应该使用哪种搜索方式
- **参数提取**：提取价格范围、类别、产品名称等关键信息
- **权重调整**：根据用户关注点动态调整字段权重

### 2. 支持的搜索类型

#### 2.1 混合搜索（Hybrid Search）
- **适用场景**：一般性的产品查询
- **特点**：结合文本搜索和向量搜索，支持过滤条件
- **示例查询**：
  - "小米14 Pro的价格"
  - "性能好的手机"
  - "适合办公的笔记本"

#### 2.2 类别搜索（Category Search）
- **适用场景**：用户明确询问某个类别的产品
- **特点**：精确匹配类别，按价格排序
- **示例查询**：
  - "智能穿戴设备有哪些？"
  - "给我看看生活电器"
  - "有哪些智能家居产品？"

#### 2.3 价格范围搜索（Price Range Search）
- **适用场景**：用户指定价格区间
- **特点**：按价格范围过滤，结果按价格排序
- **示例查询**：
  - "2000元到5000元的手机"
  - "1万以内的笔记本"
  - "500元以下的配件"

#### 2.4 相似产品搜索（Similar Products Search）
- **适用场景**：查找与某个产品相似的其他产品
- **特点**：基于 embedding 向量相似度
- **示例查询**：
  - "和小米14相似的产品"
  - "有没有类似小米手环的产品？"
  - "推荐和Redmi Note类似的手机"

### 3. 智能权重调整

工具会根据用户查询的重点自动调整字段权重：

- **商品名称_embedding**（默认 5.0）
  - 当用户明确提到产品名称时提高权重
  
- **简介_embedding**（默认 1.0）
  - 当用户关注产品功能和用途时提高权重
  
- **类别_embedding**（默认 1.0）
  - 当查询涉及多个类别比较时提高权重
  
- **详细参数_embedding**（默认 1.5）
  - 当用户关注具体参数配置时提高权重

## 工作流程

```
用户查询
    ↓
LLM 分析查询意图
    ↓
生成结构化搜索参数（JSON）
    ↓
解析并验证参数
    ↓
选择合适的搜索方法
    ↓
调用 Elasticsearch 搜索
    ↓
返回格式化结果
```

## 返回参数格式

LLM 会返回以下 JSON 格式的搜索参数：

```json
{
    "search_type": "hybrid",
    "category": "智能手机",
    "price_range": {
        "min_price": 2000,
        "max_price": 5000
    },
    "product_name": null,
    "size": 10,
    "field_weights": {
        "商品名称_embedding": 5.0,
        "简介_embedding": 1.0,
        "类别_embedding": 1.0,
        "详细参数_embedding": 1.5
    }
}
```

## 使用示例

### 示例 1：产品价格查询

**输入**：`"小米14 Pro的处理器是什么？"`

**LLM 分析结果**：
```json
{
    "search_type": "hybrid",
    "size": 10,
    "field_weights": {
        "商品名称_embedding": 8.0,
        "详细参数_embedding": 3.0
    }
}
```

**执行**：使用混合搜索，提高名称和参数权重

---

### 示例 2：价格区间查询

**输入**：`"3000到6000元之间的手机有哪些？"`

**LLM 分析结果**：
```json
{
    "search_type": "price_range",
    "price_range": {
        "min_price": 3000,
        "max_price": 6000
    },
    "category": "智能手机",
    "size": 15
}
```

**执行**：使用价格范围搜索，同时过滤手机类别

---

### 示例 3：类别查询

**输入**：`"智能穿戴设备有哪些？"`

**LLM 分析结果**：
```json
{
    "search_type": "category",
    "category": "智能穿戴",
    "size": 20
}
```

**执行**：使用类别精确搜索

---

### 示例 4：相似产品查询

**输入**：`"有没有和小米14相似的产品？"`

**LLM 分析结果**：
```json
{
    "search_type": "similar",
    "product_name": "小米14",
    "size": 10
}
```

**执行**：查找与小米14相似的产品

## 容错机制

工具包含多层容错机制：

1. **JSON 解析失败**：降级为默认混合搜索
2. **参数提取失败**：使用默认参数值
3. **搜索方法失败**：降级为基础混合搜索
4. **类别/产品不存在**：自动切换到混合搜索

## 优势

1. **智能化**：自动理解用户意图，无需用户了解底层搜索机制
2. **灵活性**：支持多种搜索方式，自动选择最优策略
3. **精准性**：动态调整权重，提高搜索相关性
4. **鲁棒性**：多层容错，保证服务稳定性
5. **可扩展**：易于添加新的搜索类型和策略

## 技术栈

- **LLM**：通义千问（qwen-plus）用于查询分析
- **Embedding**：bge-base-zh-v1.5 用于向量搜索
- **搜索引擎**：Elasticsearch 7.x
- **框架**：LangChain + LangGraph

## 性能优化建议

1. **缓存常见查询**：对高频查询缓存 LLM 分析结果
2. **并行搜索**：对于模糊查询，可以并行执行多种搜索策略
3. **结果排序**：可以添加二次排序逻辑，进一步优化结果质量
4. **用户反馈**：收集用户反馈，持续优化 prompt

## 未来改进方向

1. **多轮对话**：支持上下文理解和多轮交互
2. **个性化**：根据用户历史查询调整搜索策略
3. **A/B 测试**：测试不同 prompt 和权重配置的效果
4. **搜索解释**：向用户解释为什么返回这些结果
5. **联合搜索**：同时搜索产品和公司信息，智能融合结果

## 测试

运行测试脚本：

```bash
python test_smart_es_retriever.py
```

## 注意事项

1. 确保 Elasticsearch 服务运行在 `localhost:9200`
2. 确保已加载 embedding 模型（bge-base-zh-v1.5）
3. 确保设置了正确的 OpenAI API Key（通义千问）
4. 首次使用时，LLM 分析可能需要 1-2 秒

## 联系方式

如有问题或建议，请提交 Issue 或 Pull Request。



